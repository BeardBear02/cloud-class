server:
  port: 8000
spring:
  application:
    name: cloud-course
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/cloud_course?autoReconnect=true&useSSL=false
    username: root
    password: Liuhuzile0301
eureka:
  client:
    register-with-eureka: false # 不注册自己
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:7001/eureka/
mybatis:
    mapperLocations: classpath:mappers/*.xml
home:
  course:
    pageSize: 8
